// src/lib/schemas.ts

// This file serves as an index for all schemas in the application.
// By re-exporting from more specific files, we prevent circular dependency issues.

export * from './schemas/DataEntrySchema';
export * from './schemas/eTenderSchema';
